import { OsobyToStringPipe } from './osoby-to-string.pipe';

describe('OsobyToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new OsobyToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
